{
  "name": "ppc",
  "version": "1.0.0",
  "description": "Web Planner Path Calculator - is a sub-system of the Global Planning System that is used to store and extract planning data modeled as a graph. A graph is used to model different manufacturing processes, for example a Tree can be used to model the process of making a product as the result of the assembly of multiple components.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Giuseppe Bianchi",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.16.0",
    "express": "^4.14.0",
    "mongoose": "^4.7.7"
  }
}
